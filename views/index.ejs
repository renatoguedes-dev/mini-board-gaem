<html>
  <head>
    <link rel="stylesheet" href="/styles.css">
    <title><%= title %></title>
  </head>
  <body>

    <%- include("components/headerTitle", {title: title}) %>

    <% if (showIndex) { %>
        <%- include("components/messages", {messages: messages}) %>
        <%- include("components/addNewMessage") %>
    <% } %>

    <% if (showForm) { %>
        <%- include("components/form") %>
    <% } %>

  </body>
</html>
